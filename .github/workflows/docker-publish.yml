name: Docker

on:
  push:
    branches:
      - master

env:
  IMAGE_NAME: cli-tools

jobs:
  docker-build-push:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    name: Build and push docker image to Github Packages

    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: ./deploy.sh

      - name: Log into registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login docker.pkg.github.com -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: docker push docker.pkg.github.com/${{ github.repository }}/$IMAGE_NAME

  slack-notification:
    runs-on: ubuntu-latest
    needs: docker-build-push
    name: Send message to slack channel if docker push completed

    steps:
      - uses: actions/checkout@v2

      - name: Grab latest version for slack message
        run:  echo "::set-env name=DOCKER_TAG::$(cat versions | grep DOCKER_TAG= | head -n 1 | grep -o '".*"' | sed 's/"//g')"

      - name: Send message to Slack API
        uses: archive/github-actions-slack@v1.0.2
        with:
          slack-bot-user-oauth-access-token: ${{ secrets.SLACK_BOT_USER_OAUTH_ACCESS_TOKEN }}
          slack-channel: team-cloud
          slack-text: "cli-tools updated, the latest version is now ${{ env.DOCKER_TAG }}. Time for a docker pull team-cloud :bongo:"

      - name: Result from "Send Message"
        run: echo "The result was ${{ steps.notify.outputs.slack-result }}"
